Sub Dir関数の使い方_応用編()

Dim pth As String
Dim file As String

    pth = ThisWorkbook.Path & "\test\"
    file = Dir(pth & "*.xls?")
    
    Debug.Print file

    Do While Len(file) > 0
    
        Debug.Print file
       file = Dir()

    Loop

End Sub
